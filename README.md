# F-Secret Gallery

เว็บแกลเลอรี่สำหรับลูกค้าเลือกชมผลงาน

## Admin Login
- Username: `tatchaihot`
- Password: `741852963`

## Deploy บน Vercel
1. Import repo นี้ใน Vercel
2. Build Command: `vite build`
3. Output Directory: `dist`
4. ใส่ Environment Variables (ถ้าต้องการเปลี่ยน):
   - `ADMIN_USER` (default: tatchaihot)
   - `ADMIN_PASS` (default: 741852963)
   - `JWT_SECRET` (optional)
